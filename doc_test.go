package gangway_test

import "testing"

// TestModuleBuilds is a placeholder proving the module compiles and the
// test runner is wired up. It is replaced by real tests in later tasks.
func TestModuleBuilds(t *testing.T) {
	t.Log("module builds")
}
